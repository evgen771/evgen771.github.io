---
# Only the main Sass file needs front matter (the dashes are enough)
---

@import "minima";

.site-header {
  position: sticky;
  top: 0;
  background: white;
  z-index: 10;
}

.site-footer {
  position: fixed;
  bottom: 0;
  left: 0;
  width: 100%;
  background: #fdfdfd;
  z-index: 1000;
}
