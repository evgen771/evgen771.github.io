---
# Only the main Sass file needs front matter (the dashes are enough)
---

@import "minima";

/* Закреплённая шапка */
.site-header {
  position: sticky;
  top: 0;
  z-index: 1000;
  background-color: #fff;
}

/* Подвал обычный */
.site-footer {
  position: relative;
}
