**doas** — это минималистичная альтернатива популярной утилите `sudo` в Unix-подобных системах, разработанная проектом OpenBSD. Она предназначена для выполнения команд с повышенными привилегиями, например от имени пользователя **root**, но при этом проще по дизайну, легче в настройке и безопаснее благодаря меньшему объёму кода.

### 🔧 Основные особенности doas:
- **Простота конфигурации**: файл `/etc/doas.conf` легко читается и редактируется, синтаксис близок к естественному английскому.
- **Отсутствие сложных утилит**: в отличие от `sudo`, где для редактирования конфигурации используется `visudo`, в `doas` можно просто открыть файл в любом текстовом редакторе.
- **Меньше уязвимостей**: благодаря компактности кода, вероятность наличия критических уязвимостей снижается.
- **Гибкость**: позволяет настраивать права для пользователей и групп, указывать конкретные команды и условия выполнения.

---

### 📦 Установка doas

В большинстве дистрибутивов Linux `doas` доступен под названием **opendoas**:

- **Ubuntu/Debian** (начиная с 21.04):  
  ```bash
  sudo apt install doas
  ```

- **Arch Linux**:  
  ```bash
  sudo pacman -S opendoas
  ```

- **Fedora/CentOS**:  
  ```bash
  sudo dnf install opendoas
  ```

---

### ⚙️ Настройка doas

Создайте или отредактируйте файл `/etc/doas.conf` (только от имени root):

```bash
permit persist :wheel as root
```

Это правило разрешает всем пользователям из группы **wheel** выполнять команды от имени root без повторного ввода пароля в течение 5 минут (благодаря `persist`).

#### Примеры правил:
- Разрешить пользователю `alice` запускать `pacman` без пароля:  
  ```bash
  permit nopass alice as root cmd pacman
  ```

- Запретить конкретному пользователю, даже если он в группе:  
  ```bash
  permit :wheel as root
  deny baduser as root
  ```

> ⚠️ Важно: последнее совпадающее правило имеет приоритет. Правила читаются сверху вниз.

После редактирования установите правильные права:
```bash
sudo chown root:root /etc/doas.conf
sudo chmod 0400 /etc/doas.conf
```

Проверьте синтаксис:
```bash
doas -C /etc/doas.conf && echo "config ok" || echo "config error"
```

---

### 🖥️ Использование doas

Используется так же, как и `sudo`:
```bash
doas pacman -Syu
doas systemctl restart sshd
```

Для запуска интерактивной оболочки root:
```bash
doas -s
```

---

### 🔄 Переход с sudo на doas

Можно плавно перейти, добавив в `~/.bashrc`:
```bash
alias sudo='doas'
alias sudoedit='doas nano'
```

> ⚠️ Некоторые пакеты (например, `devtools`) жёстко зависят от `sudo`, поэтому полная замена возможна не везде.

---

### 💡 Почему стоит рассмотреть doas?

- Если вы хотите **минимализм и безопасность** — `doas` отличный выбор.
- Если вам нужны **тонкие настройки в крупных системах** — `sudo` остаётся более гибким.

На 2025 год `doas` активно используется, особенно в дистрибутивах, ориентированных на безопасность, но `sudo` по-прежнему доминирует благодаря своей универсальности.

Если хочешь, могу помочь настроить `doas` под твой дистрибутив — просто скажи, какой у тебя Linux 😊
