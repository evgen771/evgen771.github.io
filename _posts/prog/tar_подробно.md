Ниже — **подробный, пошаговый и практический разбор `tar` в FreeBSD**, с примерами **от простых к продвинутым**. Формат — как шпаргалка + объяснения, чтобы можно было сразу применять.

---

# 📦 tar в FreeBSD — полный гайд

## 1️⃣ Что такое tar

`tar` = **T**ape **Ar**chiver
Сам по себе **НЕ сжимает**, а:

* собирает файлы в один архив (`.tar`)
* может работать вместе с компрессией (`gzip`, `bzip2`, `xz`, `zstd`)

В FreeBSD используется **BSD tar** (не GNU tar, есть отличия).

---

## 2️⃣ Общий синтаксис

```sh
tar [опции] [архив] [файлы/каталоги]
```

---

## 3️⃣ Основные режимы (самое важное)

| Ключ | Назначение               |
| ---- | ------------------------ |
| `-c` | создать архив            |
| `-x` | извлечь                  |
| `-t` | показать содержимое      |
| `-f` | имя архива (ОБЯЗАТЕЛЬНО) |
| `-v` | подробный вывод          |

> ⚠️ `-f` **должен быть последним перед именем архива**

---

## 4️⃣ Создание архива (.tar)

### 📁 Архивировать каталог

```sh
tar -cf backup.tar /home/user
```

### 📄 Архивировать несколько файлов

```sh
tar -cf files.tar file1.txt file2.txt
```

### 📢 С подробным выводом

```sh
tar -cvf backup.tar /etc
```

---

## 5️⃣ Просмотр содержимого архива

```sh
tar -tf backup.tar
```

С подробностями:

```sh
tar -tvf backup.tar
```

---

## 6️⃣ Извлечение архива

### 📂 Извлечь в текущий каталог

```sh
tar -xf backup.tar
```

### 📢 С выводом файлов

```sh
tar -xvf backup.tar
```

### 📁 Извлечь в другой каталог

```sh
tar -xvf backup.tar -C /tmp/restore
```

---

## 7️⃣ Сжатие (очень важно)

### gzip (.tar.gz / .tgz)

```sh
tar -czvf backup.tar.gz /home
```

Распаковка:

```sh
tar -xzvf backup.tar.gz
```

---

### bzip2 (.tar.bz2)

```sh
tar -cjvf backup.tar.bz2 /home
```

Распаковка:

```sh
tar -xjvf backup.tar.bz2
```

---

### xz (.tar.xz)

```sh
tar -cJvf backup.tar.xz /home
```

Распаковка:

```sh
tar -xJvf backup.tar.xz
```

---

### zstd (современно, быстро)

```sh
tar --zstd -cvf backup.tar.zst /home
```

Распаковка:

```sh
tar --zstd -xvf backup.tar.zst
```

---

## 8️⃣ Извлечь один файл из архива

```sh
tar -xvf backup.tar etc/rc.conf
```

---

## 9️⃣ Исключение файлов и каталогов

### ❌ Исключить каталог

```sh
tar -cvf backup.tar /home --exclude=/home/user/.cache
```

### ❌ Несколько исключений

```sh
tar -cvf backup.tar /home \
  --exclude=.cache \
  --exclude=Downloads
```

---

## 🔟 Архивирование без абсолютных путей (ОЧЕНЬ ВАЖНО)

❌ Плохо:

```sh
tar -cvf etc.tar /etc
```

✅ Правильно:

```sh
cd /
tar -cvf etc.tar etc
```

или

```sh
tar -cvf etc.tar -C / etc
```

---

## 1️⃣1️⃣ Сохранение прав, владельцев, симлинков

```sh
tar -cvpf backup.tar /usr/local
```

| Ключ              | Значение         |
| ----------------- | ---------------- |
| `-p`              | сохранить права  |
| `--numeric-owner` | UID/GID без имён |

---

## 1️⃣2️⃣ Архивирование системы (FreeBSD)

### ⚠️ Исключая виртуальные ФС

```sh
tar -cvpf system.tar \
  --exclude=/dev \
  --exclude=/proc \
  --exclude=/tmp \
  --exclude=/mnt \
  /
```

---

## 1️⃣3️⃣ Проверка архива

```sh
tar -tf backup.tar > /dev/null
```

Если ошибок нет — архив целый.

---

## 1️⃣4️⃣ Архивирование через pipe (продвинуто)

### tar + gzip вручную

```sh
tar -cvf - /home | gzip > backup.tar.gz
```

Распаковка:

```sh
gzip -dc backup.tar.gz | tar -xvf -
```

---

## 1️⃣5️⃣ Инкрементальное архивирование

```sh
tar -cvf backup.tar --newer=2025-01-01 /home
```

Архивируются только новые файлы.

---

## 1️⃣6️⃣ Частые ошибки ❌

### ❌ Забыли `-f`

```sh
tar -cv backup.tar /home
```

### ❌ Перепутали порядок

```sh
tar -cvf /home backup.tar   # НЕПРАВИЛЬНО
```

---

## 1️⃣7️⃣ Быстрая шпаргалка

| Задача     | Команда                         |
| ---------- | ------------------------------- |
| Создать    | `tar -cvf a.tar dir`            |
| Посмотреть | `tar -tvf a.tar`                |
| Извлечь    | `tar -xvf a.tar`                |
| gzip       | `tar -czvf a.tar.gz dir`        |
| bzip2      | `tar -cjvf a.tar.bz2 dir`       |
| xz         | `tar -cJvf a.tar.xz dir`        |
| zstd       | `tar --zstd -cvf a.tar.zst dir` |

---

## 1️⃣8️⃣ tar vs dump (для FreeBSD)

| tar           | dump        |
| ------------- | ----------- |
| Универсальный | Только UFS  |
| Простой       | Системный   |
| Для файлов    | Для системы |

---

Если хочешь:

* 🔹 **сделаю PDF-шпаргалку**
* 🔹 **готовый backup-скрипт под FreeBSD**
* 🔹 **пример для /home, /etc, всей системы**
* 🔹 **сравнение BSD tar vs GNU tar**

Скажи, под какую задачу 👍
