---
layout: post
title: FFmpeg. Фильтр "Масштабирование видео".
categories: FFmpeg
---

Единственными настройками являются ширина `w` и высота `h`:

```bash
ffmpeg -i input.mp4 -vf "scale=w=800:h=600" output.mp4
```

Можете использовать математические операции для значений:

```bash
ffmpeg -i input.mkv -vf "scale=w=1/2*in_w:h=1/2*in_h" output.mkv
```
Эта команда устанавливает размер вывода равным половине (`1/2`) от размера ввода `in_w`, `in_h`.
